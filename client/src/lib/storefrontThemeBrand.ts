import type { Theme } from "@mui/material/styles";

/** Marketing accent for fills, gradients, tints, and `sx={{ bgcolor: "brand.main" }}` */
export function storefrontBrandMain(t: Theme): string {
  return t.palette.brand.main;
}
